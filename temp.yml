- name: Play Test
  hosts: localhost
  gather_facts: no
  become: true
  vars:
    #GUID: abc123    
    jom: "{{ lookup('ENV','JOM') }}"
  tasks:

    - name: Print GUID
      debug:
        var=GUID

    - name: Print JOM
      debug:
        var=jom

